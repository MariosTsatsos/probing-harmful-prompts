# probing-harmful-prompts
Probing harmful intent in LLM activations using linear classifiers. Compares base vs instruction-tuned models, evaluates layer-wise decodability, and includes controls for lexical, syntactic, and domain artefacts (cross-family splits, shuffled labels, word-order ablations) to assess whether signals go beyond bag-of-words.
